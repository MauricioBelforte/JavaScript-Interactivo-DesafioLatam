/* 
Múltiples parámetros
Ideas clave
Una función puede depender de múltiples parámetros para llevar a cabo su tarea.
Cómo trabajar con múltiples parámetros
Podemos especificar más de un parámetro en una función separándolos por comas. Por ejemplo, para crear una función que sume dos números, escribiremos:

function sumar(a, b) {
  console.log(a + b);
}
Al llamar a la función sumar también deberemos especificar dos valores, uno para cada parámetro. Por ejemplo:

sumar(5, 3); // 8 
sumar(10, 20); // 30 
Ejercicio
Crea la función restar que dependa de dos parámetros a y b. Esta función al ser llamada debe mostrar el resultado de la resta de los dos valores ingresados.
*/

/* Escribe tu código aquí */




/* Fin */

/* restar(10, 5);
restar(3, 5);
restar(9, 4);
 */


/* Escribe tu código aquí */

function restar(a,b){
    console.log(a-b);
}


/* Fin */

restar(10, 5);
restar(3, 5);
restar(9, 4);